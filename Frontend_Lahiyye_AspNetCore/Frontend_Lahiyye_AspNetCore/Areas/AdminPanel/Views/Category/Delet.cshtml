@model Category;
@{
    ViewData["Title"] = "Delet";
}
    <div class="row">
        <div class="col-12">
            <h3>@Model.Name</h3>
            <h5 class="mt-3">@Html.Raw(Model.Description)</h5>
            <p class="text-danger"> Silmek isteiyinize eminsizmi ??</p>
        </div>

    </div>
    <div class="row mt-4">
        <div class="col-lg-12">
            <form class="forms-sample" method="post">
                <button type="submit" class="btn btn-danger mt-2">Delete</button>
            </form>
        </div>
        <div class="col-lg-12 mt-3">
            <a asp-action="Index" class="btn btn-dark">Go Back</a>
        </div>
    </div>

